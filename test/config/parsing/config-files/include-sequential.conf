events {
    worker_connections 1024;
}

http {
    server {
        listen 8080;
        server_name first.example.com;
        include include-sequential-included.conf;
    }

    server {
        listen 8081;
        server_name second.example.com;
        include include-sequential-included.conf;
    }
}
